[project]
name = "mllm-backend"
version = "0.1.0"
description = "Local Multi-Agent Model Platform — Backend"
requires-python = ">=3.11"
dependencies = [
    "pywebview>=5.0",
    "httpx>=0.27",
    "psutil>=5.9",
    "docker>=7.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
]
